# Firebase (app/google-services.json and app/GoogleService-Info.plist handle this)
# No additional environment variables are required.
# Each user provides their own TMDB API key during in-app onboarding;
# it is stored in their Firestore user profile, not as an environment variable.
