# Firebase (app/google-services.json and app/GoogleService-Info.plist handle this)
# Functions environment — set via v2 params: firebase functions:secrets:set TMDB_API_KEY
# or provide via environment variable files
TMDB_API_KEY=your_tmdb_api_key_here
